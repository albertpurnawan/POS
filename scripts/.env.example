# Example env file for scripts/deploy.sh
# Copy to .env or deploy.env and adjust values

# Required
IMAGE_NAME=your-dockerhub-username/pos
IMAGE_TAG=latest
CONTAINER_NAME=pos
PORT=33000
CONTAINER_PORT=80  # allowed: 80 or 443

# Optional
BIND_ADDR=0.0.0.0
# DOCKER_USER=
# DOCKER_PASS=
# RUN_ARGS=-e FOO=bar

