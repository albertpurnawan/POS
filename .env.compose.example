# Example env file for docker-compose.yml and scripts/deploy_compose.sh

# Frontend image and container names
FRONTEND_IMAGE=albertpurnawan1/pos-frontend
FRONTEND_NAME=pos-frontend
FRONTEND_PORT=33000

# Vite build-time envs (optional)
VITE_API_URL=https://api.example.com

# Backend (optional, enable by BACKEND_ENABLED=1)
BACKEND_ENABLED=0
BACKEND_IMAGE=albertpurnawan1/pos-backend
BACKEND_NAME=pos-backend
BACKEND_PORT=4000

# Docker registry login (optional if images are public)
# DOCKER_USER=
# DOCKER_PASS=

